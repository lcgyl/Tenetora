interface:
  display_name: "Tenetora Align"
  short_description: "Align engineering decisions before execution"
  default_prompt: "Use $tenetora-align to explicitly align this governed engineering task's goal, scope, tradeoffs, risks, approval boundaries, and acceptance criteria before planning or implementation."
policy:
  allow_implicit_invocation: false
